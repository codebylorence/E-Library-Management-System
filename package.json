{
  "name": "e-library-management-system",
  "version": "1.0.0",
  "description": "CvSU Carmona E-Library Management System",
  "scripts": {
    "install:all": "npm install --prefix back && npm install --prefix front",
    "build": "npm install --prefix back && npm install --prefix front && npm run build --prefix front",
    "start": "node back/server.js",
    "dev": "node back/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
